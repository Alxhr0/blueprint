# Pinned base image digests (tracked by Renovate).
# promote-<base>.yml pins the current :testing digest here; tag-<base>-stable.yml
# re-tags it as :stable.
images: []

# Pinned binary downloads (managed by Renovate)
downloads:
  # renovate: datasource=github-releases depName=ublue-os/uupd
  uupd: "v1.4.0"
